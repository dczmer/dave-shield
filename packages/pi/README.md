# Configuration

> NOTE: I wasn't able to figure out a clever way to manage the config as part of the flake, since it needs to be writable so it can't be in the store.

```bash
mkdir ~/.pi
cd ~/.pi
ln -s /REPO_PATH/packages/pi/config agent
```
